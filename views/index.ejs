<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <link rel="shortcut icon" href="assist/image" type="image/x-icon">
   <link rel="stylesheet" href="output.css">
    <title>Note Book</title>
</head>
<body class=" bg-slate-900 text-white p-4 h-screen">
    <div class="form bg-slate-900 w-full h-72 ">
        <form action="/note" method="post">
            <input type="text" placeholder="Titel "  class="bg-slate-800 h-8 p-2 rounded-md text-white w-full" name="title" required>
           <textarea placeholder="Enter your Notes here  " name="content" class="w-full mt-4 p-2 rounded-md h-36  bg-slate-800 "></textarea>
            <input type="submit" class="btn rounded-md mt-3 ml-5 w-28 bg-blue-600" value="Create" >
        </form>
    </div>
    <div class="w-full  bg-slate-800 text_white rounded-md  p-4 grid grid-cols-3 gap-4">
       <%if(files.length>0){ %>
       <% files.forEach(file => { %>
        <div class="box bg-slate-600  rounded-md p-4">
            <h2><%=file%></h2>
           <a href="/file/<%=file%>" class="text-blue-700">read more...</a>
        </div>
        <%}) %>
        <% }else{ %>
            <h2>Emty not file exist</h2>
            <%}%>
    
       
    </div>

</body>
</html>